const PAGE_TYPE = {
  ARTICLE: 'Article', // must match Contentful value
  CATEGORY: 'Category', // must match Contentful value
  HOME: 'Home',
};

const PAGE_TYPES = [PAGE_TYPE.ARTICLE, PAGE_TYPE.CATEGORY, PAGE_TYPE.HOME];

export default {
  PAGE_TYPE,
  PAGE_TYPES,
};
